export class tableClass {
    createdAt: string;
    name: string;
    avatar: string;
    id: string;

    constructor(createdAt: string, name: string, avatar: string, id: string) {
        this.createdAt = createdAt;
        this.name = name;
        this.avatar = avatar;
        this.id = id;
    }
}